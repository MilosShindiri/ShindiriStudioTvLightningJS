export const DIMENSIONS = {
  SCREEN_WIDTH: 1920,
  SCREEN_HEIGHT: 1080,
};

const W = DIMENSIONS.SCREEN_WIDTH;
const H = DIMENSIONS.SCREEN_HEIGHT;

export const SCREEN = {
  w: W,
  h: H,
  w_half: W / 2,
  h_half: H / 2,
  w_third: W / 3,
  h_third: H / 3,
  w_quarter: W / 4,
  h_quarter: H / 4,
};

export default DIMENSIONS;
